---
"@authhero/widget": patch
---

Restyle the "Last used" social-connection badge: the pill now sits on the button's top-right corner straddling the border (instead of overlapping the label), uses the theme primary color (`--ah-color-last-used` to override), and the matching button gets a highlighted border via a new `button-social-last-used` part. The widget demo gained a "Last used" Badge toggle to preview it.
