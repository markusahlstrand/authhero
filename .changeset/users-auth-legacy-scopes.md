---
"authhero": patch
---

Accept legacy `auth:read`/`auth:write` scopes on management-api users routes alongside the granular `read:users`/`update:users`/`create:users`/`delete:users` scopes. Transitional measure until all clients are updated.
