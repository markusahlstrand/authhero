---
"@authhero/admin": patch
---

Add a "Show last used badge" toggle to the prompts Settings tab so `show_last_used_connection` can be enabled per tenant from the admin UI instead of only via the Management API.
