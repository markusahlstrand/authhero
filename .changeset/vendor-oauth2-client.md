---
"authhero": patch
---

Vendor a minimal OAuth2 client and drop the deprecated `arctic` dependency (and its transitive `@oslojs/*` packages). Wire behavior for all social-login strategies (Google, Apple, Facebook, GitHub, Microsoft Entra, Vipps, generic oauth2/oidc) is unchanged.
