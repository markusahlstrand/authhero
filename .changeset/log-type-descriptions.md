---
"@authhero/adapter-interfaces": minor
---

Add shared `logTypeDescriptions`, `logTypeCategories`, `getLogTypeDescription`, `getLogTypeCategory`, and `LogCategory` exports so admin UIs can render log type labels and pick icons without duplicating the lookup tables. Also added the missing `INFORMATION` ("i") log type code.
