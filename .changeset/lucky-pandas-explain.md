---
"@authhero/docs": patch
---

Document recently shipped features that were missing from the docs: OIDC Back-Channel Logout 1.0 (new standards page, plus fixing the stale "only ships RP-Initiated Logout" note in the conformance doc), the opt-in "Last used" connection badge on u2 login screens, checkpoint (`from`/`take`) pagination on the Management API, and the Auth0-parity `blocked` user flag.
