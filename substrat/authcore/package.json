{
  "name": "authhero-auth-core-standin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stand-in for the AuthHero auth core as a Substrat vertical — one entitlement-probe operation so cross-vertical provisioning + the entitlements projection can be proven end-to-end before the real auth core lands. NOT the real OAuth runtime.",
  "substrat": {
    "slug": "authhero-auth-core",
    "name": "AuthHero Auth Core (stand-in)"
  },
  "scripts": {
    "build:worker": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@substrat-run/adapter-cloudflare": "^0.31.0",
    "@substrat-run/contracts": "^0.31.0",
    "@substrat-run/kernel": "^0.31.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.6.0"
  }
}
