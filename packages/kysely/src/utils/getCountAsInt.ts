// Re-exported from @authhero/adapter-interfaces, the canonical home for
// helpers shared between the SQL adapters. Kept as a default export because
// all existing imports use `import getCountAsInt from ...`.
export { getCountAsInt as default } from "@authhero/adapter-interfaces";
